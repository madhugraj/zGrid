# API keys (comma-separated)
POLICY_API_KEYS=supersecret123,policyvalyavar

# CORS origins (Lovable preview + local dev)
CORS_ALLOWED_ORIGINS=https://zgrid-feature-flow.lovable.app,http://localhost:5173,http://localhost:3000

# GGUF model path (host path for local run, container path for docker)
# LOCAL RUN (host path):
LLAMAGUARD_GGUF=policy_service/models/llamaguard-7b-gguf/llamaguard-7b.Q4_K_M.gguf
# DOCKER RUN (container path; set in docker-compose service env):
# LLAMAGUARD_GGUF=/app/models/llamaguard-7b.Q4_K_M.gguf

# Runtime tuning
N_THREADS=0          # 0 = auto
N_CTX=2048
POLICY_ACTION_ON_FAIL=refrain   # refrain|filter|reask
